spring:
  # DB 접속 정보 설정
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/test_db?serverTimezone=UTC
    username: root
    password: "Mandarin2675!"

  # JPA 설정
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  #서버 설정
  server:
    servlet:
      session:
        #timeout:1800
        tracking-modes: cookie