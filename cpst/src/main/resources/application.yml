spring:
  # DB 접속 정보 설정
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://sugangtest.c7euaq44ad1f.us-east-2.rds.amazonaws.com:3307/sugang
    username: admin
    password: "00000000"
  # JPA 설정
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

server:
  port: "8000"
  servlet:
    session:
      #timeout:1800
      tracking-modes: cookie